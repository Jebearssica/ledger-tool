# 记账工具

当前目录下包含一个记账工具的完整实现，它应该具备如下功能：

* 能够按月或按日同步支付宝+微信的收支，最好是自动化的，如果无法做到自动化考虑设计一个半自动化的流程，例如我是否能够通过支付宝/微信将收支导出成某种文件，然后这个工具支持导入这个文件自动同步记账
* 同理，除开支付宝/微信，最好也支持银行卡的收支同步
* 上述两点实现后，可能会出现导入后多个流水之间存在重复项，要能识别这些重复项避免重复记录收支
* 需要区分开销类别，例如交通/饮食/娱乐/日用品/房租等等
* 需要识别银行卡与微信/支付宝之间的互相转账，并且忽略掉这样的互相转账
* 需要识别银证转账这种用于投资的部分，投资的部分不要记录在收支中，而是单独记录
* 需要支持安卓端安装运行
* 需要支持 Windows 端运行，可以是一个可交互的 html 页面而无需实际的一个程序
* 最好支持云端存储数据，但需要注意隐私保护
  * 如果使用 GitHub Action 进行每月或每日的信息同步与备份，需要设计加解密部分防止其他人能够在公开仓库中阅读到用户的开销数据
  * 需要确保 GitHub Action 针对这种每日每月的同步不会触发监控导致我的账号被 block